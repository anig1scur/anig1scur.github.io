if is_home() || is_archive() || is_year()  || page.type === 'about'
    .head
        .head__inner
            h1
                a(href=theme.menu.Home) #{config.title}
            p #{config.description}
